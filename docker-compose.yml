version: "3.2"

services:
  nginx:
    build:
      context: nginx
      dockerfile: Dockerfile
    ports:
    - 80:80
    restart: always
  
  web-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:80
    
  client:
    image: hernantos/taptrack_client_1
    ports:
    - 3000:80